

- given an graph in memory and containing a signature, validate the signature
- given a serialisation in rdf containing signature, validate the signature


- change size of edge types
- change thickness of edges: penwidth
    

Validation

- test that a opm graph instance is compatible with owl ontology

- test that a opm graph instance is compatible with xml schema
  noteschema opm_v1.01 hardcoded in validator

- signatures of opm graphs

